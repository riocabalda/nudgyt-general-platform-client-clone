export type Subscription = {
  id: string
  name: string
  features: string
  price: string
  time: string
  selected: boolean
}

const subscriptionService = {}

export default subscriptionService
